import { InhospPatientType } from '@/apis/inhosp';

interface HooksProps {
  liveData?: InhospPatientType;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default (_: HooksProps) => {
  return undefined;
};
